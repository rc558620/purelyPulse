/**
 * 路由守卫层。
 *
 * 这个文件不负责声明 path，也不负责渲染 Routes。
 * 它只负责一件事：把"页面进入前需要经过的限制条件"封装成可复用包装器。
 *
 * 这样 routeDefinitions 里就可以只写：
 * - 哪个页面要套哪种守卫
 * 而不用在 definitions 里塞大段 JSX。
 */

// 暂无守卫，后续按需在此添加。
export {};
